[phases.setup]
nixPkgs = ["php84", "nodejs_20", "composer"]

[phases.build]
cmds = ["composer install --optimize-autoloader --no-scripts --no-interaction", "npm ci", "npm run build"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"